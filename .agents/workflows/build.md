---
description: Build the Electron app for Windows distribution
---

// turbo-all

1. Run type checks and build the Electron bundle:
```
pnpm build
```

2. Package the app for Windows:
```
pnpm build:win
```
